apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: read-statefulset
spec:
  selector:
    matchLabels:
      app: read
  serviceName: read-service
  replicas: 2
  template:
    metadata:
      labels:
        app: read
    spec:
      initContainers:
      - name: init-read
        image: mariadb
        env:
        - name: MYSQL_ROOT_PASSWORD
          value: "gutsandgun"
        command:
        - bash
        - "-c"
        - |
          set -ex
          mkdir -p /var/lib/mysql/read
          # Generate mysql server-id from pod ordinal index.
          [[ $HOSTNAME =~ -([0-9]+)$ ]] || exit 1
          ordinal=${BASH_REMATCH[1]}
          echo [mysqld] > /etc/mysql/conf.d/server-id.cnf
          # Add an offset to avoid reserved server-id=0 value.
          echo server-id=$((100 + $ordinal)) >> /etc/mysql/conf.d/server-id.cnf
          # Copy appropriate conf.d files from config-map to emptyDir.
          if [[ $ordinal -eq 0 ]]; then
            cp /mnt/config-map/primary.cnf /etc/mysql/conf.d
          else
            cp /mnt/config-map/replica.cnf /etc/mysql/conf.d
          fi
        volumeMounts:
        - name: master
          mountPath: /etc/mysql/master
        - name: config-map
          mountPath: /mnt/config-map
        - name: data
          mountPath: /var/lib/mysql
        - name: conf
          mountPath: /etc/mysql/conf.d
      - name: clone-mysql
        image: gcr.io/google-samples/xtrabackup:1.0
        command:
        - bash
        - "-c"
        - |
          set -ex
          until mysql -u root -e "SELECT 1" -pgutsandgun; do sleep 1; done
          # Skip the clone if data already exists.
          if [[ $ordinal -eq 0 ]]; then
            mysqldump -u root -pgutsandgun --all-databases > /etc/mysql/master/master.sql
          else
            mysql -u root \
                  -pgutsandgun \
                  -e "$(</etc/mysql/master/master.sql), \
                          MASTER_HOST='read-statefulset-0', \
                          MASTER_USER='root', \
                          MASTER_PASSWORD='gutsandgun', \
                          MASTER_CONNECT_RETRY=10; \
                        START SLAVE;" || exit 1
          fi        
        volumeMounts:
        - name: master
          mountPath: /etc/mysql/master
        - name: data
          mountPath: /var/lib/mysql
        - name: conf
          mountPath: /etc/mysql/conf.d
      containers:
      - name: read
        image: mariadb
        env:
        - name: MYSQL_ROOT_PASSWORD
          value: "gutsandgun"
        ports:
        - name: read
          containerPort: 3306
        volumeMounts:
        - name: master
          mountPath: /etc/mysql/master
        - name: data
          mountPath: /var/lib/mysql
        - name: conf
          mountPath: /etc/mysql/conf.d
        resources:
          limits:
            memory: 2Gi
          requests:
            cpu: 500m
            memory: 1Gi
      volumes:
      - name: master
        nfs:
          server: 172.30.1.100
          path: /k8s/db_config/read/master
      - name: conf
        nfs:
          server: 172.30.1.100
          path: /k8s/db_config/read/config
      - name: config-map
        configMap:
          name: read-config
  volumeClaimTemplates:
  - metadata:
      name: data
    spec:
      storageClassName: nfs-storageclass
      accessModes: ["ReadWriteMany"]
      resources:
        requests:
          storage: 100Gi